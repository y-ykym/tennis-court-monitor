// ============================================================
// 監視対象の公園と監視条件の定義
// 公園を増減したり監視条件を変えるときは、このファイルだけを編集する
//
//   name    : サイト表記の公園名(通知にもこのまま表示される)
//   code    : サイトの公園セレクトのvalue(調査結果は docs/site-notes.md 参照)
//   keyword : 取得した施設名との部分一致キーワード(「公園」の有無など表記揺れに備えて短めに)
//   weekdayStartHours : 平日に監視する枠の開始時刻(時)。空配列なら平日は監視しない
//   priority: フェーズ3 自動予約で同じ利用日に候補が複数あるときの公園の優先順(小さいほど先に試す)
//
// 土日祝は全公園・全時間帯を監視する(lib/filter.js参照)
// ============================================================
const PARKS = [
  { name: '猿江恩賜公園', code: '1040', keyword: '猿江', weekdayStartHours: [19], priority: 2 },
  { name: '亀戸中央公園', code: '1050', keyword: '亀戸中央', weekdayStartHours: [], priority: 3 },
  { name: '大島小松川公園', code: '1160', keyword: '大島小松川', weekdayStartHours: [], priority: 1 },
];

// ---- フェーズ3 自動予約の設定(要件は docs/PROMPT_フェーズ3_自動予約.md §2) ----
const AUTO_BOOKING = {
  // ペナルティ日数(サイトの penaltyday)。利用日 <= 今日 + この日数 の枠は自動予約しない(取消でペナルティが付くため)
  PENALTY_DAYS: 3,
  // 利用日 = 今日 + PENALTY_DAYS + 1(= +4 日。ペナルティなしで取り消せるのが今日 23:59 までの枠)は、この時刻(JST "HH:MM")以降は自動予約しない
  // (取れても取消の猶予が無いため。要件 #11)。+5 日以降の枠には締切なし。判定は見つけた時と「予約」の直前の両方で行う
  LAST_DAY_DEADLINE: '23:35',
  // 利用日ごとの上限件数(手動で取った予約も数える。テニスベアのイベントは数えない)。2026-09-17 に 2 → 1
  MAX_PER_DAY: 1,
  // 隣接の判定(2026-09-24 追加): その人の同じ日の予定(都の予約・テニスベアのイベント)と時間が重なる候補は自動予約しない(同時にはできない)。
  // 端が接する(間の空きが ADJACENT_GAP_MINUTES 分以下)候補は、同じ公園でなければ自動予約しない(別の場所だと移動が間に合わないため)。
  // 0 = ちょうど接している(前の予定の終了 = 候補の開始、または候補の終了 = 次の予定の開始)ときだけ
  ADJACENT_GAP_MINUTES: 0,
  // テニスベアのイベントの終了時刻が取れないときに仮定する長さ(分)
  DEFAULT_EVENT_MINUTES: 120,
  // 同じ利用日に候補が複数あるときの時間帯(開始時刻)の優先順。2026-09-17 に変更
  HOUR_PRIORITY: [11, 9, 15, 13, 17, 19],
  // 予約者: 平日(利用日基準)は B、土日祝は A
  PERSON_BY_DAY: { weekday: 'B', holiday: 'A' },
  // 申込人数
  PEOPLE: 2,
  // Pi 側の空き照会の間隔(ミリ秒)と、その下限(これより短くしない。要件 §7)。
  // POLL_INTERVAL_MS は POLL_SCHEDULE のどの時間帯にも当たらない時間(= 21:00〜翌 1:00。取消が集中し争奪に勝つ必要がある時間)の間隔
  POLL_INTERVAL_MS: 60 * 1000,
  MIN_POLL_INTERVAL_MS: 30 * 1000,
  // 時間帯(JST)ごとの間隔。相手のサーバーへの負荷を減らすため、空きが出にくい時間は広げる(2026-09-17 本人決定)
  //   1:00〜 7:00 … 3 分(ほぼ出ない)   7:00〜21:00 … 2 分(散発)   それ以外(21:00〜翌 1:00)… POLL_INTERVAL_MS(1 分)
  POLL_SCHEDULE: [
    { FROM: '01:00', TO: '07:00', POLL_INTERVAL_MS: 3 * 60 * 1000 },
    { FROM: '07:00', TO: '21:00', POLL_INTERVAL_MS: 2 * 60 * 1000 },
  ],
  // Pi の最後の照会がこの時間以内なら「自動予約は生きている」とみなす(Worker が Pi の /auto/status を直接聞いて判定。Actions が通知を絞る条件)。
  // 最も広い 3 分間隔 + 照会の所要でも切れないよう 5 分(worker/src/auto.js の ALIVE_WITHIN_MS と同じ値)
  ALIVE_WITHIN_MS: 5 * 60 * 1000,
};

module.exports = { PARKS, AUTO_BOOKING };
