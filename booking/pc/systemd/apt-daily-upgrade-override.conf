# apt の自動更新(unattended-upgrades)を毎朝 4:00(±10 分)に動かす。
# 既定は 6:00(±60 分)だが、再起動時刻を 4:30 にしているので、更新 → 同じ朝に再起動、の順になるよう前に出す。
# pi-init.sh §7 が /etc/systemd/system/apt-daily-upgrade.timer.d/override.conf に置く
[Timer]
OnCalendar=
OnCalendar=*-*-* 04:00
RandomizedDelaySec=10m
