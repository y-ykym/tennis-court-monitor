{
  "name": "tennis-reservation-bot",
  "version": "0.1.0",
  "private": true,
  "description": "LINEグループで「よやく」と送ると予約サイトの予約一覧(+テニスベアの予定)を返信し、キャンセル・自動予約の設定・ペナルティ予告も担う Cloudflare Workers ボット(フェーズ1.5/1.6/3/4/5)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format pretty",
    "test": "node --test \"test/**/*.test.mjs\"",
    "send-test-event": "node scripts/send-test-event.mjs",
    "probe-tennisbear": "node scripts/probe-tennisbear.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  },
  "dependencies": {
    "japanese-holidays": "^1.0.10"
  }
}
