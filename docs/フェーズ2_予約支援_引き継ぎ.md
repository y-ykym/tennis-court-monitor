# フェーズ2「予約支援(半自動予約)」引き継ぎドキュメント

新しいセッションでフェーズ2の設計・実装を始めるための引き継ぎ。方針は検討・合意済みで、
このドキュメントの「決定事項」に従って進めてよい。前提知識は「現状のシステム」と
「既存資産」を読めば揃う。

## 1. フェーズ2でやること(合意済みの方針)

**「新しい空きのLINE通知 → ボタンをタップ → 人間はreCAPTCHAと確定だけ」の半自動予約。**

- LINEのFlex通知カードの各枠に「予約する」ボタン(URI)を付ける
- タップすると予約支援サーバーが起動し、Playwright制御のheadful Chromeが
  **ログイン〜対象枠の選択〜予約確認画面の直前まで自動遷移**する
- その画面を **noVNC** でスマホのブラウザにそのまま配信する
- **人間がやるのは reCAPTCHA のチェックと「確定」ボタンのタップだけ**
- 完全自動予約はしない(下記「越えてはいけない一線」参照)

### 越えてはいけない一線(この順で強い制約)

1. **reCAPTCHAの機械突破・回避は絶対にしない**。人間が自分の予約のために自分で解く設計のみ
2. **最終確定ボタンは必ず人間が押す**(自動で押さない)
3. 「事前に枠を選択して待機」はしない(セッション切れ・仮押さえ問題)。**タップ起点のオンデマンド遷移**のみ
4. 着手前に**都の予約システムの利用規約**で自動操作の扱いを確認する(未確認。要ユーザー確認)

## 2. アーキテクチャ(決定)

```
[監視系(稼働中・変更しない)]
cron-job.org --(3分おき workflow_dispatch)--> GitHub Actions(check.js)
  --(空き照会 JSON API)--> 都営予約サイト
  --(新規空きのみ Flex通知)--> LINEグループ(A・B・ボット)
  --(同時に /warmup を叩く)--> 予約支援サーバー ← フェーズ2で追加する連携

[予約支援系(新設)]
LINE Flexカード「予約する」ボタン(署名付きURL、枠情報入り)
  --> Cloud Run(Webアプリ + Playwright headful Chrome + Xvfb + noVNC)
  --> ログイン〜対象枠選択まで自動遷移 --> noVNCでスマホに画面配信
  --> 人間が reCAPTCHA + 確定
```

### ホスティング: **GCP Cloud Run**(決定)

- ゼロスケール(使わない間は0円)+ WebSocket対応 + HTTPS標準装備がこの用途に最適
- **max instances = 1** + セッションアフィニティ(`/book` とnoVNCのWebSocketが同一インスタンスに届く必要がある)
- 認証情報(都サイトの利用者番号・パスワード)は **GCP Secret Manager**
- リージョンは東京(asia-northeast1)

### コールドスタート対策: **通知時ウォームアップ**(決定)

- Cloud Runのコールドスタート(20〜60秒)+自動遷移(30〜60秒)は、素朴に作ると
  人間の手動予約(2〜3分)と互角になってしまう
- 対策: **GitHub Actionsが通知を送る瞬間に、Cloud Runの `/warmup` も叩く**
  (check.js または workflow に1ステップ追加)。人間がタップする頃には起動済みになり、
  体感の待ちは自動遷移の残り(〜30秒)だけになる
- ウォームアップは「監視対象の空きが出たとき」しか走らないので、0円運用は崩れない

### セキュリティ(決定)

- Flexボタンの URL には**署名付きトークン**(枠情報+有効期限をHMAC署名)を入れ、
  第三者がURLを踏んでも起動しないようにする
- noVNC にはパスワードを設定する
- 都サイトの認証情報はSecret Managerのみ。リポジトリ・LINEメッセージには絶対に入れない

## 3. ユーザーの利用フロー(合意済みのUX)

1. LINE通知が届く(この時点で裏でウォームアップ開始)
2. Flexカードの行きたい枠の「予約する」ボタンをタップ
3. ブラウザで待機画面(数秒〜30秒。裏でログイン〜枠選択まで自動遷移)
4. noVNCで予約サイトの確認画面がスマホに表示される
5. 人間: reCAPTCHAチェック → 「確定」タップ
6. 完了。予約完了メールは都のシステムから通常どおり届く

失敗系: 先に取られていたら画面がそのまま「予約あり」表示になる(誤予約は構造上起きない)。
タップが遅れてセッションが切れていたら、待ちが1〜2分に伸びるだけで流れは同じ。

## 4. 着手前に確認・計測すべきこと(未解決の宿題)

1. **利用規約**: 自動操作(遷移の自動化)の扱い。違反ならアカウント停止リスクがあるため最重要
2. **reCAPTCHAの出現箇所**: ログイン時か予約確定時か(自動遷移の「止めどころ」が変わる)。
   ユーザーは予約フローにreCAPTCHAがあると認識している
3. **セッション寿命の実測**(15〜30分と推定)と、枠選択が仮押さえ扱いになるか
   (ペナルティ制度があるため、選択放置の影響を確認)
4. AとBが同時にタップした場合の挙動をどうするか(同一インスタンス・同一画面を共有する)

## 5. 既存資産(実装時に必ず参照)

| 資産 | 場所 | 使いどころ |
|---|---|---|
| **ログイン実装(重要)** | `worker/src/site.js` | フェーズ1.5のLINEボットが**予約サイトへのログインと予約一覧取得を既に実装済み**。ログインのPOST先・パラメータ・セッションの扱いはここから流用できる |
| サイトのAPI・画面構造の調査記録 | `docs/site-notes.md` | 画面遷移、公園コード(猿江=1040/亀戸=1050/大島小松川=1160)、種目コード(テニス人工芝=1000_1030)、施設コード(例: 猿江=10400030)、Shift_JIS、サーバー不安定パターン |
| 公園定義 | `lib/config.js` | name/code/keyword/weekdayStartHoursの一元管理 |
| Flexカード生成 | `lib/notify.js` の `buildFlexMessage()` | ここに枠ごとの「予約する」ボタン(署名付きURI action)を追加する。**バブル10KB制限に注意**(現在12件上限。ボタン追加でサイズが増えるため上限の再計算が必要) |
| 通知の送信元 | `check.js` | ウォームアップ呼び出しの追加箇所 |
| LINE Webhook基盤 | `worker/src/` (Cloudflare Workers) | フェーズ1.5のボット。LINE署名検証等の実装例として参照可 |

## 6. 現状のシステム(フェーズ1/1.5、稼働中)

- **監視(フェーズ1)**: cron-job.orgから3分おきに`workflow_dispatch`でGitHub Actionsを起動
  (**GitHubのschedule(cron)はこのアカウントで極端に間引かれるため使っていない**。
  5分指定で実効3〜4時間。検証用リポジトリ `y-ykym/cron-canary` が観測装置として存続中で、
  正常化を確認したらscheduleに戻す手順がREADME運用メモにある)
- 取得はPlaywright不使用。サイト内部のJSON APIを素のHTTP(Node標準fetch)で叩く
- 通知はグループLINE宛て(`LINE_USER_ID` SecretにグループID `C〜`)のFlex Message
- state.jsonはActionsボットが自動コミット(ローカル作業前に `git pull` 必須)
- **フェーズ1.5**: `worker/` にCloudflare WorkersのLINEボット(「よやく」で予約一覧をFlexで返信)。
  Secretsは `wrangler secret put` で登録済み(トークン・利用者番号・パスワード)
- コストは全体で0円。LINE無料枠は月200通。cron-job.orgのPATは期限1年(失効時はcron-job.orgから失敗メール)

## 7. 実装の進め方(推奨順序)

1. 宿題1〜3(規約・reCAPTCHA箇所・セッション寿命)の確認。特に規約NGなら中止判断
2. ローカルで予約フローの自動遷移をPlaywrightで組む(**確定は絶対に押さない**。
   確認画面到達までをテストし、直前で止める)。`worker/src/site.js` のログイン処理を移植
3. コンテナ化(Chrome+Xvfb+noVNC+Webアプリ)してCloud Runへ。署名付きURL・Secret Manager・
   max instances=1・セッションアフィニティを設定
4. `lib/notify.js` に予約ボタン追加、`check.js`(またはworkflow)にウォームアップ呼び出し追加
5. モック枠でエンドツーエンド確認(通知→タップ→noVNC→CAPTCHA画面表示まで)

## 8. 予約サイトの基本情報(再掲)

- 入口: https://kouen.sports.metro.tokyo.lg.jp/web/index.jsp (2023年リニューアル版。URLは旧来のまま)
- レスポンスはShift_JIS(Windows-31J)
- サーバーは不安定(一時的な502、セッション未認識の200)。リトライ前提で組む
- 定期メンテ: 毎月27日12:00〜28日8:45、年末年始12/28 12:00〜1/4 8:45(`lib/maintenance.js`)
- 予約枠は2時間単位(9/11/13/15/17/19時開始)、カレンダーのセルクリックが予約導線

## 9. 進捗メモ

### 2026-09-03 着手(宿題の調査)

調査の詳細は `docs/site-notes.md`「フェーズ2 追加調査」。

- **宿題1 規約**: サイトに「利用規約」は無く、利用条件は「都立公園のスポーツ施設 利用案内」(PDF)。
  自動操作を明示的に禁じる記述は無し。関係条項は「パスワードを第三者に教えない/貸し借り禁止」
  「本人が利用する予約以外を申し込まない」「一操作2件まで」。→ **ユーザーの最終判断待ち**
- **宿題2 reCAPTCHA**: ログイン画面の組み込みは reCAPTCHA **v3(見えない方式・現在は無効)**。
  予約フロー側の有無はログイン後の画面なので `booking/scripts/explore-flow.mjs` で確認する。
  v3 なら「チェックを入れる」工程は無く、人間の操作は「確定を押す」だけになる
- **宿題3 セッション寿命**: 実測済み(site-notes 参照)。**10分以内は確実、15分前後で切れ始め、30分で全滅**。
  設計上は「安全な寿命=10分」として扱う。「選択=仮押さえか」は explore-flow.mjs が選択前後・
  申込画面到達後・ログアウト後の空き面数を別セッションから取って表示するので、それで判定する
- **設計上の確認**: URL(`;jsessionid=`)でのセッション引き継ぎは不可 → スマホに直接ログイン済み画面を
  渡す近道は無く、noVNC 方式(決定事項)で進めるのが正しいと確認できた
- **成果物**: `booking/`(フェーズ2のディレクトリ。Playwright 導入済み)、
  `booking/scripts/explore-flow.mjs`(本人ログイン→検索→枠選択→「予約する」→次画面で停止。確定は押さない)

### 2026-09-03 STEP 1 完了(予約フローの実機確認)

- explore-flow.mjs で本人ログイン → 検索 → セル選択 → 「予約」→ **予約内容確認画面(prwea1000.jsp)に到達**して停止。
  詳細は site-notes「予約フローの実機確認結果」
- **宿題2 決着**: reCAPTCHA は予約内容確認画面で有効(v3・見えない方式)。「予約」ボタン押下時にサイトのJSが
  自動でトークンを取る。人間がチェックを入れる工程は無い → 人間の操作は「人数入力 →『予約』→ 確認OK」
- **宿題3 決着**: セル選択・確認画面到達のどちらも仮押さえにならない(他セッションから見た空き面数が変わらない)
- 自動遷移の止めどころ = 予約内容確認画面。人数の入力までは自動で埋め、「予約」と確認ダイアログは人間が押す
- 注意: ログイン直後に「東京都からのお知らせ」モーダルが出ることがあり、閉じないと検索フォームが進まない

### 2026-09-03 STEP 2 完了(通しテスト・方針変更)

- **方針変更(ユーザー指示)**: 「最終確定ボタンは人間が押す」を改め、**「予約」ボタンと確認ダイアログの OK はスクリプトが押す**。
  人間の出番は「目に見える reCAPTCHA(チェックボックス/画像選択)が出た場合に解く」ときだけ。
  reCAPTCHA の機械突破・回避をしない原則は変わらない(サイトの JS が v3 トークンを取るのを妨げない、偽装しない)
- 通しテストの結果: **予約成立、reCAPTCHA v3 は自動操作ブラウザからのクリックでも通過**(site-notes「通しテストの結果」)。
  テスト予約は人間が取消済み(ペナルティ期間外)
- 設計への影響: 人間が画面を見る必要がほぼ無くなるため、**noVNC は「reCAPTCHA チャレンジが出たときだけ」の保険**に格下げできる。
  通常経路は「LINE のボタン → サーバーが予約完了まで自動実行 → 結果(予約番号・料金)を LINE に返す」で成立する。
  ただし v3 のスコアはサーバー環境(データセンターIP・ヘッドレス相当)で下がる可能性があり、Cloud Run 上での通過確認が必須

### 2026-09-03 実装(booking/src)とローカル dry-run

- 構成の決定(ユーザー合意): **全工程を Playwright(ヘッドレス Chromium)で通す**(API直叩き+ブラウザの混成にはしない。
  reCAPTCHA v3 の点数はブラウザ内の一連の振る舞いで決まるため、実機で通った条件をそのまま本番にする)。
  ホスティングは **GitHub Actions**(LINE ボタン → 既存 Worker → repository_dispatch → Actions → 結果を LINE push)。
  Cloud Run / noVNC は作らない(遅すぎる・拒否が頻発する場合に再検討)
- 成果物: `booking/src/reserve.js`(予約実行本体)、`booking/scripts/reserve-cli.mjs`(CLI。一時エラーは新ブラウザで最大2回)、
  `booking/scripts/notify-result.mjs`(結果の LINE 通知カード)、`.github/workflows/reserve.yml`(repository_dispatch / workflow_dispatch)
- ローカル dry-run(ヘッドレス): 予約内容確認画面まで **約39秒**(ログイン13秒・検索18秒・選択2秒・確認画面7秒)。
  遅さはサイトの静的ファイル配信(1ファイル3〜7秒)が原因で、画像・フォント・チャットボットの読み込みを止めて 47→39秒に短縮
- 一時エラーの実例: 「予約」を押しても空き状況画面(prwrc2000)が返る(サイトがセッション上の選択を認識しない)。CLI の再試行で対応
- 保護: reCAPTCHA の JS が読み込まれる前に「予約」を押すとサイトの JS が「トークン無し」で送ってしまうため、読み込みを待ち、
  読み込めなければ予約せず error で終える


### 2026-09-03(夜) reCAPTCHA 検証の結論と方式の再検討

- Actions(DC IP)からの dry-run 計測: **合計19回中 v3(チェック不要)17回・v2(要チェック)2回**。v2 は当日最初の2回のみで、
  以降16回連続 v3(機械操作9回 + 人間らしい操作7回)。詳細は site-notes「検証やり直し」「人間らしい操作版の検証」
- 一時「6/6 で v2」と記録・報告したのは集計ミス(訂正済み)。ユーザーはその誤情報を前提に noVNC を選んだため、正しい数字で再選択を依頼中
- 人間らしい操作(humanize)は導入済み(コミット 7fa87bf)。所要 59〜89秒。v2 抑止効果は未確定(v2 自体がまれ)
- 現時点の推奨: **GitHub Actions で完全自動を主経路、v2 が出た回だけ「サイトで手動」を LINE で案内**。noVNC/Cloud Run は作らない


### 2026-09-03(夜・確定) v2 は送信後に出る。DC IP の完全自動は不成立

- 人間らしい操作版で Actions から実予約 ×2 → **2回とも「予約」送信後に v2 チェックボックスが出て不成立**。
  dry-run(送信前の画面)では v3 のみに見えるため、**dry-run の v3 率は成功率の指標にならない**(先の 17/19 は無効)
- 実予約の成績: Actions 0/3、Mac(自宅回線)1/1。詳細は site-notes「決定的な発見」
- 結論: **完全自動は住宅 IP(ユーザーの Mac 等)でのみ成立**。DC IP で動かすなら、送信後の v2 を人間が解く半自動(noVNC)が必要。
  humanize(人間らしい操作)では DC IP の判定は覆らなかった
- 方式は再びユーザー判断待ち: (a) Mac を self-hosted runner にして完全自動、(b) 半自動 noVNC(Cloud Run)、(c) Mac 常駐


### 2026-09-03(夜) 方式決定: 半自動 noVNC(ユーザー判断・確定)

- 前提(確定): DC IP では「予約」送信後に v2 チェックボックスが必ず出る(実予約 0/3)。人間らしい操作でも覆らない
- 構成: **Cloud Run の1コンテナ**(Node Webアプリ + Xvfb + headful Chromium + x11vnc + noVNC)。
  LINE の署名付きURL → `/book?token=` → サーバーがログイン〜予約確認画面(人数入力まで)を自動遷移 →
  待機画面から noVNC に切替 → **人間が「予約」→ 出てきた v2 チェック → 再度「予約」** → 完了画面をサーバーが検知して結果表示
- noVNC の WebSocket 橋渡し(websockify 相当)は Node(`ws`)で自前実装し、Cloud Run の単一ポートに収める
- `reserve.js` に `onConfirm` フック(予約確認画面で人間に制御を渡し、完了画面への遷移を待つ)を追加して流用
- 実装ファイル: `booking/src/token.js`(HMAC署名URL)、`booking/server/server.mjs`、`booking/Dockerfile`、`booking/docker-entrypoint.sh`
- 手順: ローカル Docker でビルド・動作確認 → GCP(gcloud導入・プロジェクト・Secret Manager・Cloud Run デプロイ。ユーザー手作業、クレカ登録が要る)
  → `lib/notify.js` に「予約する」ボタン(署名付きURL)


### 2026-09-03(夜) noVNC サーバー実装・ローカル Docker で疎通確認済み

- 実装: `booking/server/server.mjs`(/book → 予約者 A/B 選択 → 自動遷移 → /wait → /vnc → /result、WebSocket 橋渡し内蔵)、
  `booking/src/token.js`、`booking/Dockerfile`+`docker-entrypoint.sh`(Xvfb 600x1000、x11vnc localhost:5900)、`booking/deploy.sh`
- `lib/notify.js`: 枠ごとの「予約」ボタン(BOOKING_BASE_URL/BOOKING_SIGNING_SECRET があるときだけ)。10KB に収まるよう件数を自動調整
  (ボタン付きは約8件が上限)。`check.js`: 通知と同時に /warmup
- ローカル Docker で `/smoke`(認証情報なしでトップページを表示)→ noVNC で画面が映ることを確認(2026-09-03 20:39)。
  はまりどころ: npm の @novnc/novnc は CommonJS のみ → esbuild で ESM に束ね、既定エクスポートは `mod.default` を見る
- 未確認: 本物のログイン〜予約確認画面〜人間の操作(Cloud Run 上で初めて通す)。Cloud Run の DC IP なので v2 が出る前提
- 次: GCP 側(ユーザー手作業): gcloud 導入 → プロジェクト作成・課金有効化 → API 有効化 → Secret Manager 登録 → `booking/deploy.sh`
  → GitHub Secrets に BOOKING_BASE_URL / BOOKING_SIGNING_SECRET → mock-test で通知カードにボタンが付くことを確認 → 実枠で通し


### 2026-09-03(夜) 高速経路を実装・ローカル確認済み

- `booking/src/site-http.js`(HTTP でログイン〜枠選択)+ `reserve.js` の `prepared` オプション(Cookie 移植 → 確認画面 POST)。
  サーバー・CLI(`--fast`)は高速経路を先に試し、一時エラーなら全ブラウザ方式に自動で切り替える
- ローカル dry-run: HTTP 約20秒 + ブラウザ約15秒(site-notes「高速経路の実測」)。予約確認画面到達・人数入力まで確認
- Docker イメージはローカルで最新コードにて再ビルド済み(`tennis-booking:dev`)
- 次: GCP 配置(ユーザー手作業)。STEP 1 = `brew install --cask google-cloud-sdk`


### 2026-09-03(夜) Cloud Run で通し確認(人間の操作の直前まで)成功

- リビジョン tennis-booking-00002。署名URL → 予約者選択 → 高速経路(ブラウザ内 fetch)→ 予約内容確認画面(人数入力済み)まで **38秒**、
  noVNC の WebSocket 橋渡し OK、/abort で予約せず終了(abandoned)。詳細は site-notes「Cloud Run 上の高速経路」
- 未確認: **人間がスマホで noVNC を操作して「予約」→ v2 チェック → 予約成立**(次の STEP。成立後はユーザーがキャンセル)
- 通知側は GitHub Secrets(BOOKING_BASE_URL / BOOKING_SIGNING_SECRET)登録済みなので、次の空き通知から「予約」ボタンが付く

### 次にやること

1. コミット・push(ワークフローは main に無いと起動できない)
2. GitHub Secrets 登録: SITE_USER_A / SITE_PASS_A / LABEL_A(必要なら B も)。LINE の2件は既存
3. Actions で workflow_dispatch(dry_run=true)→ 通れば dry_run=false で本番通し(予約→人間がキャンセル)。
   データセンター IP からの reCAPTCHA v3 通過を確認する
4. `lib/notify.js` に「予約する」ボタン(postback, 署名付き)、`worker/` に postback → 確認カード → repository_dispatch の分岐、
   `check.js` は変更不要(ウォームアップ不要になった)

### 2026-09-03(夜) GCP 準備の進捗

- gcloud CLI 583.0.0 導入、`yykm.engineer@gmail.com` でログイン済み
- プロジェクト **tennis-booking-c46c52c5**(名前 tennis-booking)を作成し、gcloud の既定に設定。課金アカウントは未作成(ユーザーがコンソールで作成・紐付け)
- 次: 課金の紐付け → API 有効化 → Secret Manager 登録 → `booking/deploy.sh`
- 2026-09-03 夜: 課金アカウント(無料トライアル ¥47,813・90日)を紐付け。API(run/cloudbuild/artifactregistry/secretmanager/billingbudgets)有効化。
  予算アラート「tennis-booking 月500円」作成。署名鍵を生成し Secret Manager `booking-signing-secret` と GitHub Secrets `BOOKING_SIGNING_SECRET` に同じ値を登録(値はどこにも記録していない)。
  Cloud Run 実行 SA(<番号>-compute@)に secretAccessor 付与。次: site-user-a / site-pass-a / label-a の登録(ユーザー入力)→ deploy.sh
- 2026-09-03 夜: **Cloud Run に配置完了**。サービス `tennis-booking`(asia-northeast1)、URL https://tennis-booking-czjicxowfq-an.a.run.app
  (別名 https://tennis-booking-597048084380.asia-northeast1.run.app)。Secret Manager: booking-signing-secret / site-user-a / site-pass-a / label-a。
  GitHub Secrets に BOOKING_BASE_URL / BOOKING_SIGNING_SECRET を登録 → 次回の空き通知から「予約」ボタンが付く
